/// Windows USB/raw print support for [PrintWorker].
///
/// This plugin is registered automatically when included as a dependency.
/// No manual initialization is required.
class PrintWorkerWindowsPlugin {
  const PrintWorkerWindowsPlugin._();
}
